{
  "name": "jira-pr-updater",
  "version": "1.0.0",
  "description": "GitHub Action to update PR title and body with Jira issue details",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build jira-pr-updater.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "jira",
    "pull-request"
  ],
  "author": "Rajasimman S",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
